spring:
    application:
        name: zero-disruptor-boot
    profiles:
        active: dev
server:
    port: 8080

management:
    endpoints:
        web:
            exposure:
                include: "*"
    endpoint:
        shutdown:
            enabled: true